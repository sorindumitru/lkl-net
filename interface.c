#include <interface.h>

interface_t* alloc_interface()
{
	interface_t* interface = malloc(sizeof(interface_t));

	return interface;
}

int lkl_init_initerface(const interface_t* interface)
{

	return 0;
}
