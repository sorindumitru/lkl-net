hypervisor {
	device {
		type hub;
		hostname hub1;
		config alfa;
		port 50001;
	}

	device {
		type hub;
		hostname hub2;
		config beta;
		port 50002;
	}

	device {
		type hub;
		hostname hub3;
		config gamma;
		port 50003;
	}

	device {
		type switch;
		hostname London;
		config tests/stp/switch.london;
	}

	device {
		type switch;
		hostname Manchester;
		config tests/stp/switch.manchester;
	}

	device {
		type switch;
		hostname Glasgow;
		config tests/stp/switch.glasgow;
	}
}
